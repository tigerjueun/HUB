pje0914@ParkJuEun:~ $ STAMP=$(date +%Y%m%d-%H%M%S)

# 1. 현재 새 OS config 원본 백업
sudo cp -a /boot/firmware/config.txt \
"/boot/firmware/config.txt.bak.aegis-$STAMP"

echo "Backup created:"
echo "/boot/firmware/config.txt.bak.aegis-$STAMP"

# 2. 자동 카메라 감지 OFF
sudo sed -i -E \
's/^[[:space:]]*camera_auto_detect[[:space:]]*=.*/camera_auto_detect=0/' \
/boot/firmware/config.txt

# 3. Pi 5의 두 포트에 IMX219를 수동 지정
sudo tee -a /boot/firmware/config.txt >/dev/null <<'EOF'

# ===== AEGIS dual IMX219 manual camera config =====
[all]
dtoverlay=imx219
dtoverlay=imx219,cam0
EOF

# 4. 적용 내용 확인
echo
echo "===== CAMERA CONFIG AFTER EDIT ====="
sudo grep -nE \
'camera_auto_detect|dtoverlay=imx219' \
/boot/firmware/config.txt
[sudo] password for pje0914: 
Backup created:
/boot/firmware/config.txt.bak.aegis-20260808-144822

===== CAMERA CONFIG AFTER EDIT =====
17:camera_auto_detect=0
58:dtoverlay=imx219
59:dtoverlay=imx219,cam0
pje0914@ParkJuEun:~ $ 
