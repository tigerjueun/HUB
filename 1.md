echo "=== HOST ==="
hostname
hostname -I

echo
echo "=== CAMERAS ==="
rpicam-hello --list-cameras

echo
echo "=== SSH ==="
sudo systemctl enable --now ssh
systemctl is-active ssh

echo
echo "=== PYTHON ==="
python3 --version
