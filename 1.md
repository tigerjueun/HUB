cd ~/aegis

pkill -f sender_TCP_5560.py 2>/dev/null || true
pkill -f sender_FIXED_2.py 2>/dev/null || true
pkill -9 rpicam-vid 2>/dev/null || true

cp src/sender_TCP_5560.py \
   src/sender_TCP_5560.py.bak_before_cam_delay





python3 -m py_compile src/sender_TCP_5560.py



   python3 - <<'PY'
from pathlib import Path

p = Path("src/sender_TCP_5560.py")
s = p.read_text(encoding="utf-8")

old = '''    cam0 = RPiCamStream(
        0, args.width, args.height,
        args.fps, args.rotation, args.sensor_mode
    )
    cam1 = RPiCamStream(
        1, args.width, args.height,
        args.fps, args.rotation, args.sensor_mode
    )
'''

new = '''    cam0 = RPiCamStream(
        0, args.width, args.height,
        args.fps, args.rotation, args.sensor_mode
    )

    # Pi 5 dual IMX219: avoid simultaneous libcamera initialization.
    print(">> Camera 0 started; waiting before Camera 1 init...", flush=True)
    time.sleep(1.5)

    cam1 = RPiCamStream(
        1, args.width, args.height,
        args.fps, args.rotation, args.sensor_mode
    )
'''

if old not in s:
    raise SystemExit("ERROR: camera initialization block not found")

s = s.replace(old, new, 1)
p.write_text(s, encoding="utf-8")

print("CAMERA START DELAY PATCH OK")
PY
