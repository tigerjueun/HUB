pje0914@ParkJuEun:~ $ cd ~/aegis

echo "===== FILES ====="
find . -maxdepth 2 -type f -print

echo
echo "===== PY COMPILE ====="
python3 -m py_compile config_turret.py src/sender_FIXED_2.py

echo
echo "===== SENDER HELP ====="
python3 src/sender_FIXED_2.py --help | head -n 40
===== FILES =====
./src/sender_FIXED_2.py
./config_turret.py

===== PY COMPILE =====

===== SENDER HELP =====
usage: sender_FIXED_2.py [-h] [--profile {calibration,runtime}]
                         [--laptop-ip LAPTOP_IP] [--port PORT] [--width WIDTH]
                         [--height HEIGHT] [--fps FPS]
                         [--jpeg-quality JPEG_QUALITY]
                         [--sensor-mode SENSOR_MODE] [--rotation ROTATION]
                         [--sender-id {rpi1}]
                         [--left-logical-id LEFT_LOGICAL_ID]
                         [--right-logical-id RIGHT_LOGICAL_ID]
                         [--verbose-dots] [--stats-interval STATS_INTERVAL]
                         [--allow-resolution-mismatch]

rpi1 stereo sender for wired ZMQ/JPEG streaming.

options:
  -h, --help            show this help message and exit
  --profile {calibration,runtime}
                        calibration=1280x720 capture profile, runtime=low-
                        latency RUNTIME_STREAM profile
  --laptop-ip LAPTOP_IP
                        Laptop wired Ethernet IP.
  --port PORT
  --width WIDTH         Override selected profile width.
  --height HEIGHT       Override selected profile height.
  --fps FPS             Override selected profile FPS.
  --jpeg-quality JPEG_QUALITY
                        Override selected profile JPEG quality.
  --sensor-mode SENSOR_MODE
                        Override selected profile rpicam sensor mode.
  --rotation ROTATION   Override selected profile rotation.
  --sender-id {rpi1}    Fixed logical sender id for this Raspberry Pi.
  --left-logical-id LEFT_LOGICAL_ID
                        Logical camera id for physical CSI camera 0 in the
                        outgoing payload.
  --right-logical-id RIGHT_LOGICAL_ID
                        Logical camera id for physical CSI camera 1 in the
                        outgoing payload.
  --verbose-dots        Print one character for each sent/dropped packet.
  --stats-interval STATS_INTERVAL
                        Seconds between sender status logs.
  --allow-resolution-mismatch
pje0914@ParkJuEun:~/aegis $ 

